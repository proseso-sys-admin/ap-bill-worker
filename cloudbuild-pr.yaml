# PR-check: validates code before merge. Does NOT deploy.
steps:
  - name: 'node:20-slim'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -euo pipefail
        npm ci
        npm run check
        npm test

options:
  logging: CLOUD_LOGGING_ONLY
